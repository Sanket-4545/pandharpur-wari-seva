export const metadata = {
  title: 'Lost Baggage Management | Admin Dashboard',
  description: 'Track and manage lost items reported by volunteers.',
};

export default function AdminLostItemsLayout({ children }) {
  return <>{children}</>;
}
